ORG 100H
.MODEL SMALL
.DATA
STR1 DB "HeLLO CLASS!$"
MSG1 DB "Vowel found$"
MSG2 DB "Vowel not found$"

.CODE
MAIN PROC
MOV AX, @DATA
MOV ES, AX    

CLD  
LEA DI, STR1 

MOV AL, 'A'

    SCAN:
    CMP [DI], '$'
    JE FORE
    SCASB
    JE PRINT
    JMP SCAN 

FORE:
CLD  
LEA DI, STR1 

MOV AL, 'E'
SCANE:
CMP [DI], '$'
JE FORI
SCASB
JE PRINT
JMP SCANE 

FORI:
CLD  
LEA DI, STR1 

MOV AL, 'I'
SCANI:
CMP [DI], '$'
JE FORO
SCASB
JE PRINT
JMP SCANI

FORO:
CLD  
LEA DI, STR1 

MOV AL, 'O'
SCANO:
CMP [DI], '$'
JE FORU
SCASB
JE PRINT
JMP SCANO

FORU:
CLD  
LEA DI, STR1 

MOV AL, 'U'
SCANU:
CMP [DI], '$'
JE FORaa
SCASB
JE PRINT
JMP SCANU 

FORaa:
CLD  
LEA DI, STR1 

MOV AL, 'a'
SCANaa:
CMP [DI], '$'
JE FORee
SCASB
JE PRINT
JMP SCANaa 

FORee:
CLD  
LEA DI, STR1 

MOV AL, 'e'
SCANee:
CMP [DI], '$'
JE FORii
SCASB
JE PRINT
JMP SCANee 

FORii:
CLD  
LEA DI, STR1 

MOV AL, 'i'
SCANii:
CMP [DI], '$'
JE FORoo
SCASB
JE PRINT
JMP SCANii

FORoo:
CLD  
LEA DI, STR1 

MOV AL, 'o'
SCANoo:
CMP [DI], '$'
JE FORuu
SCASB
JE PRINT
JMP SCANoo

FORuu:
CLD  
LEA DI, STR1 

MOV AL, 'u'
SCANuu:
CMP [DI], '$'
JE PRINT_N
SCASB
JE PRINT
JMP SCANuu 


PRINT:
MOV AH, 9
LEA DX, MSG1
INT 21H
JMP EXIT
PRINT_N:
MOV AH, 9
LEA DX, MSG2
INT 21H
JMP EXIT
EXIT:
MOV AH, 4CH
INT 21H
MAIN ENDP